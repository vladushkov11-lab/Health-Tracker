# Health-Tracker
Health Tracker (FastAPI + Docker + JWT) Микросервисный бэкенд для трекера здоровья с авторизацией через HttpOnly cookies, статистикой и тестами.  Стек: FastAPI, PostgreSQL, Docker, JWT, Pytest  Запуск: docker-compose up -d → http://localhost:8001/docs
